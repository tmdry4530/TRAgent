scalp:
  max_leverage: 40
  position_size: 0.3
  stop_loss: 0.015
  take_profit: 0.05

swing:
  max_leverage: 10
  position_size: 0.5
  stop_loss: 0.05
  take_profit: 0.25
  trailing_stop: 0.07

risk:
  daily_loss_limit: 0.1
  consecutive_loss_cooldown: 3
  event_blackout_minutes: 30
  llm_confidence_threshold: 0.6

signals:
  # Lowered thresholds for testing
  liquidation_threshold: 5000000   # $5M (test)
  funding_rate_threshold: 0.0002   # 0.02% (test)
  volume_multiplier: 1.5           # (test)
  ema_periods: [7, 25, 99]
  rsi_period: 14
  rsi_entry_min: 30                # RSI entry zone min (test)
  rsi_entry_max: 70                # RSI entry zone max (test)
